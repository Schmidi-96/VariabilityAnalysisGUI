<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="at.variabilityanalysisgui.controller.Controller">
    <bottom>
        <HBox alignment="CENTER" spacing="20" style="-fx-padding: 15;">
            <Button fx:id="upButton" onAction="#handleHierarchyUpAction" prefWidth="80" text="&lt;-">
                <tooltip><Tooltip text="Previous Item" /></tooltip>
            </Button>
            <Button fx:id="confirmButton" onAction="#handleConfirmAction" prefWidth="80" text="OK">
                <tooltip><Tooltip text="Confirm Decision" /></tooltip>
            </Button>
            <Button fx:id="downButton" onAction="#handleHierarchyDownAction" prefWidth="80" text="-&gt;">
                <tooltip><Tooltip text="Next Item" /></tooltip>
            </Button>
        </HBox>
    </bottom>
    <right>
        <ScrollPane fx:id="detailScrollPane" fitToWidth="true" managed="false" prefWidth="350" visible="false">
            <VBox fx:id="detailsPane" spacing="10" style="-fx-padding: 10;">
                <Label style="-fx-font-weight: bold; -fx-font-size: 14px;" text="Details" />
            <HBox prefHeight="30.0" prefWidth="200.0">
               <children>
                      <Label fx:id="detailGroupNameLabel" text="Name: " wrapText="true" />
                  <TextField fx:id="detailGroupNameTextField" />
               </children>
            </HBox>
            <Label fx:id="detailLocationLabel" style="-fx-font-weight: bold;" text="Location" />
            <TextArea fx:id="detailLocationTextArea" prefHeight="200.0" prefWidth="200.0" />
                <Label fx:id="detailOccurrenceLabel" style="-fx-font-weight: bold;" text="Occurrences:" />
                <ListView fx:id="detailOccurrencesListView" prefHeight="80" />
                <Label style="-fx-font-weight: bold;" text="Elements:" />
                <ListView fx:id="detailElementsListView" prefHeight="200" />
                <Button onAction="#handleCloseDetails" text="Close Details" />
            </VBox>
        </ScrollPane>
    </right>
   <center>
      <VBox prefHeight="200.0" prefWidth="100.0" BorderPane.alignment="CENTER">
         <children>
              <HBox alignment="CENTER_LEFT" spacing="10" style="-fx-padding: 10;">
                  <Button fx:id="filterButton" onAction="#handleFilterAction" text="Filter" HBox.hgrow="NEVER">
                      <tooltip>
                     <Tooltip text="Filter Options (Not Implemented Yet)" />
                  </tooltip>
                  </Button>
                  <TextField fx:id="searchTextField" promptText="Search..." HBox.hgrow="ALWAYS" />
              </HBox>
              <TreeView fx:id="featureTreeView" style="-fx-padding: 5;" />
         </children>
      </VBox>
   </center>
   <top>
        <MenuBar BorderPane.alignment="CENTER">
            <menus>
                <Menu text="File">
                    <items>
                        <MenuItem onAction="#handleLoadAction" text="Load" />
                        <MenuItem fx:id="saveDecisionsMenuItem" disable="true" onAction="#handleSaveAction" text="Save" />
                        <SeparatorMenuItem />
                        <MenuItem onAction="#handleExit" text="Exit" />
                    </items>
                </Menu>
            </menus>
            <BorderPane.margin>
                <Insets bottom="5.0" />
            </BorderPane.margin>
        </MenuBar>
   </top>

</BorderPane>
